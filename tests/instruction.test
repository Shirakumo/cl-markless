! set author a
~~
(root)

! set line-break-mode hide
a
b
~~
(root (paragraph "ab"))

! info a
~~
(root)

! disable header
~~
(root)

! disable header
# a
~~
(root (paragraph "# a"))

! disable header
# a
! enable header
# a
~~
(root (paragraph "# a") (header (1) "a"))